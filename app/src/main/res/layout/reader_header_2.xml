<?xml version="1.0" encoding="utf-8"?>
<android.support.v7.widget.Toolbar
	android:id="@+id/readerToolbarHeader"
	xmlns:android="http://schemas.android.com/apk/res/android"
	android:layout_width="match_parent"
	android:layout_height="wrap_content"
	android:alpha=".9"
	android:background="#cc000000"
	android:elevation="2dp"
	android:visibility="visible"
	>

	<RelativeLayout
		android:layout_width="fill_parent"
		android:layout_height="wrap_content"
		android:maxHeight="20dp"
		>


		<ImageButton
			android:id="@+id/refresh_button"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_alignParentEnd="true"
			android:background="@drawable/clear_button_background"
			android:src="@drawable/ic_refresh_white_24dp"
		    android:paddingEnd="10dp"
			/>

		<ImageButton
			android:id="@+id/screen_orient_button"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:background="@drawable/clear_button_background"
			android:src="@drawable/ic_screen_rotation_white_24dp"
			android:layout_toLeftOf="@id/refresh_button"
			android:paddingEnd="10dp"
			/>

		<!--TODO.. get a new icon -->
		<ImageButton
			android:id="@+id/vertical_scroll_toggle"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:background="@drawable/clear_button_background"
			android:src="@drawable/ic_collections_bookmark_white_24dp"
			android:layout_toLeftOf="@id/screen_orient_button"
			android:paddingEnd="10dp"
			/>

		<TextView
			android:id="@+id/chapterTitle"
			android:layout_width="wrap_content"
			android:layout_height="wrap_content"
			android:layout_alignParentStart="true"
			android:layout_centerInParent="true"
			android:layout_gravity="center_horizontal"
			android:layout_toLeftOf="@id/vertical_scroll_toggle"
			android:ellipsize="middle"
			android:singleLine="true"
			android:text="Loading Chapter.."
			android:textColor="@color/white"
			android:textSize="16sp"
			android:textStyle="bold"
			/>
	</RelativeLayout>
</android.support.v7.widget.Toolbar>

